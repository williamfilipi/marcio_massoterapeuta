<section id="inicio" class="relative min-h-screen flex items-center justify-center py-20">
  <!-- Background image with CSS zoom-pan animation -->
  <div class="absolute inset-0 -z-20 overflow-hidden">
    <div class="w-full h-full animate-zoom-pan">
      <img
        src="/hero.avif"
        alt="Ambiente Spa relaxante Ã¢â‚¬â€œ Marcio Trombeta Massoterapia"
        class="w-full h-full object-cover"
        loading="eager"
        fetchpriority="high"
        referrerpolicy="no-referrer"
      />
    </div>
    <div class="absolute inset-0 bg-black/40 backdrop-blur-[2px]"></div>
    <div class="absolute inset-0 bg-gradient-to-b from-black/60 via-transparent to-black/60"></div>
  </div>

  <div class="max-w-5xl mx-auto px-6 relative z-10 text-center">
    <!-- Tagline -->
    <div class="flex items-center justify-center space-x-3 mb-8 hero-anim" style="animation-delay: 0.1s">
      <div class="w-8 h-[1px] bg-white/50"></div>
      <span class="text-[10px] font-bold text-white uppercase tracking-[0.5em]">Alta Performance em Bem-estar</span>
      <div class="w-8 h-[1px] bg-white/50"></div>
    </div>

    <!-- H1 -->
    <h1 class="text-6xl md:text-8xl font-serif leading-tight mb-8 text-white hero-anim" style="animation-delay: 0.3s">
      Toque <span class="italic font-light">Consciente</span>.<br />
      Cura <span class="font-light">Profunda</span>.
    </h1>

    <!-- Sub-headline -->
    <p class="text-lg md:text-xl text-white/90 font-light leading-relaxed mb-10 max-w-2xl mx-auto hero-anim" style="animation-delay: 0.5s">
      A fusão perfeita entre técnicas milenares e ciência moderna.
      Especialista em Shiatsu, Relaxante e Tântrica na Zona Norte de SP.
    </p>

    <!-- CTA button -->
    <div class="flex justify-center hero-anim" style="animation-delay: 0.7s">
      <a
        href="#tecnicas"
        class="px-10 py-5 border border-white/30 text-white rounded-full font-bold text-xs uppercase tracking-widest hover:bg-white hover:text-olive transition-all duration-500 flex items-center justify-center backdrop-blur-sm"
      >
        Explorar Serviços
      </a>
    </div>
  </div>

  
</section>

<style>
  .hero-anim {
    opacity: 0;
    animation: fadeInUp 0.9s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }
</style>

